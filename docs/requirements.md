# Requirements

## Supported Platform
- macOS host (Darwin)

## Host Requirements
- Built-in macOS tools:
  - `diskutil`
  - `plutil`

## Build Requirements
- Go toolchain in `PATH`

## Container Requirements
- Docker Desktop
- `docker compose`
